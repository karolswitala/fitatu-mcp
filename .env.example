# --- Authentication ---
# This server is now multi-user. Fitatu credentials are NOT configured here —
# each MCP client authenticates per-request via one of:
#   1. Authorization: Basic base64(email:password)        (static client config)
#   2. X-Fitatu-Session: <session_id>                     (after fitatu_login tool call)
# The server NEVER persists credentials or tokens to disk.

# --- Storage ---
# Inside the container; the compose volume `fitatu_data` mounts on /data.
FITATU_DB_FILE=/data/fitatu_nutrition.db

# --- Server tuning ---
LOG_LEVEL=INFO
MCP_ENABLE_DNS_REBINDING_PROTECTION=false
# Comma-separated host:port allowlist used when DNS rebinding protection is on.
# MCP_ALLOWED_HOSTS=localhost,localhost:*,fitatu-mcp,fitatu-mcp:*
# How long today's cache is treated as fresh before re-syncing on read tools.
FITATU_TODAY_TTL_SECONDS=300

# --- Session pool tuning (optional) ---
# How long an idle FitatuClient lives in memory before eviction (default: 30 days).
# FITATU_SESSION_TTL_SECONDS=2592000
# Max number of concurrent logged-in Fitatu users cached in memory (default: 64).
# FITATU_SESSION_POOL_MAX=64

# --- Cluster overrides (optional) ---
# Reads use the locale cluster (pl-pl); writes use the canonical app cluster (www).
# Do NOT include "/api" suffix — the client appends it as needed.
# FITATU_BASE_URL_READ=https://pl-pl.fitatu.com
# FITATU_BASE_URL_WRITE=https://www.fitatu.com

# --- Optional ---
# Set to true to enable destructive tools (delete_custom_product, delete_meal_item).
# Default is false so accidental LLM-triggered deletes cannot land.
FITATU_ALLOW_DELETE=false
# Override the api-secret header (public client identifier; default ships in fitatu_client.py).
# Only set if Fitatu rotates the value.
# FITATU_API_SECRET=

# --- compose.yml knobs ---
# Override which image compose pulls; leave default to build locally.
# FITATU_MCP_IMAGE=ghcr.io/pawelharacz/fitatu-mcp:latest
FITATU_MCP_PORT=8000
GENERIC_TIMEZONE=Europe/Warsaw
